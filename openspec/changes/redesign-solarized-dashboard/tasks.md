# Tasks: главная-дашборд + тема Solarized Light

## 1. Тема Solarized Light

- [x] 1.1 Заменить CSS-переменные в `BaseLayout.astro` (`:root`) на палитру Solarized Light (bg `#fdf6e3`, bg-soft `#eee8d5`, border, text `#586e75`, heading `#073642`, muted `#93a1a1`, accent `#268bd2`, green `#859900`, cyan `#2aa198`)
- [x] 1.2 Обновить типографику: заголовки — цвет `--heading` и weight 700–800, вторичный текст — `--muted`; шрифт не менять
- [x] 1.3 Перекрасить header, карточки фаз, прогресс-бары, чекбоксы (accent-color), таблицы, blockquote, inline-code в палитру; добавить hover-состояния на cyan/base2 без layout-сдвигов
- [x] 1.4 Проверить подсветку кода (Shiki) на страницах фаз; при конфликте с палитрой задать совместимую тему в `astro.config.mjs`

## 2. Чистка плана

- [x] 2.1 Удалить из `src/content/plan/roblox-learning-plan.md` все блоки `> 📘 Подробная инструкция:` перед фазами
- [x] 2.2 Удалить раздел «Отслеживание прогресса» со статичной таблицей ⬜ из плана
- [x] 2.3 Убрать из шапки плана ссылки на шпаргалки (или привести к простому тексту) — навигация к ним есть на главной

## 3. Страница `/plan/`

- [x] 3.1 Создать `src/pages/plan.astro`: рендер `plan` коллекции через `<Content />` внутри BaseLayout с `pageSlug={plan.id}` (чекбоксы остаются интерактивными)

## 4. Главная-дашборд

- [x] 4.1 Переписать `src/pages/index.astro` без `<Content />`: общий прогресс, сетка карточек фаз, блок шпаргалок, ссылка «Полный план» → `/plan/`
- [x] 4.2 Карточка фазы: номер, название, прогресс «N/M», тонкий внутренний прогресс-бар; клиентские состояния (не начата / в процессе / завершена) через `countDone`
- [x] 4.3 Добавить агрегированный общий прогресс: сумма `countDone` всех фаз против суммы `totalSteps`, обновление при загрузке страницы
- [x] 4.4 Стили дашборда в Solarized Light (сетка карточек, адаптивность на мобильных)

## 5. Верификация

- [x] 5.1 Прогнать сценарии спеков вручную: переходы с главной, prev/next, чекбоксы на `/phases/*` и `/plan/`, пересчёт общего прогресса после отметки шагов
- [x] 5.2 `npm run build` проходит успешно

## 6. Доработка по обратной связи

- [x] 6.1 Увеличить базовый размер шрифта в `BaseLayout.astro`
- [x] 6.2 Тема подсветки кода — тёмный фон (solarized-dark) вместо solarized-light
- [x] 6.3 Удалить дублирующую навигацию из md-файлов фаз (строка «Предыдущая фаза» в начале и курсивная строка prev/next в конце)
- [x] 6.4 Увеличить заголовок на главной и расширить вводное описание
- [x] 6.5 Добавить favicon (SVG) и подключить в BaseLayout
- [x] 6.6 Превратить header в понятную навигацию: бренд + явные ссылки «Главная» и «Полный план»

## 7. Страница плана: только чтение

- [x] 7.1 Скрыть чекбоксы шагов на `/plan/` (восстановить обычные маркеры списка)
- [x] 7.2 Добавить статус-бейдж «Изучено / В процессе N/M / Не начата» к каждому заголовку фазы на `/plan/`, вычисляемый из реального прогресса соответствующей фазы
- [x] 7.3 Обновить дельту спеки `site-content-rendering` (требование о статусах на странице плана)
- [x] 7.4 Пометить пройденные пункты списков плана (первые N по порядку: зелёный маркер, приглушение текста)
