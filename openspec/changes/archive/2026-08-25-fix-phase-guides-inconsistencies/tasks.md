## 1. phase-4-checkpoints.md — чекпоинты

- [x] 1.1 Переписать CheckpointManager: отслеживание касаний + `player.RespawnLocation = checkpoint` вместо ручного телепорта из CharacterAdded; fallback на явно названный StartSpawn
- [x] 1.2 Перенести создание папки `Checkpoints` (и StartSpawn) в шаг перед скриптом; добавить таймаут в WaitForChild с warn
- [x] 1.3 Исправить текст про «ближайший SpawnLocation» на корректное описание выбора движком; убрать/переписать пункт плана «Отключить стандартный спавн» в согласии с новым подходом
- [x] 1.4 Починить нумерацию: подсекция «4.2 Настрой SpawnLocation для respawn» внутри Шага 3 → перенумеровать (3.x), Шаг 4 остаётся 4.x

## 2. phase-5-gui.md и phase-5-5-remote.md — кнопка возрождения

- [x] 2.1 В phase-5: у кнопки RespawnButton оставить hideDeathScreen + явная пометка «подключим к серверу в Фазе 5.5»
- [x] 2.2 В phase-5.5: заменить абстрактный пример ResetEvent на RespawnEvent — сервер вызывает `player:LoadCharacter()`, клиентская кнопка шлёт FireServer; указать, что это замыкает кнопку из Фазы 5

## 3. phase-2-killbrick.md — примеры кирпичей

- [x] 3.1 SlowBrick: сохранять исходный WalkSpeed при касании, восстанавливать его после task.wait(3); добавить debounce-защиту и проверку humanoid.Health > 0
- [x] 3.2 PoisonBrick: реализовать заявленный периодический урон (цикл while игрок стоит на кирпиче или повтор по RATE) либо убрать неиспользуемый RATE и поправить комментарий
- [x] 3.3 Таблица ошибок: исправить строку «Kill Brick не убивает | Script вместо LocalScript | Убедись что это Script» на согласованную пару причина→решение

## 4. phase-6-datastore.md — leaderstats и сохранение чекпоинта

- [x] 4.1 В полном DataStoreManager оставить создание leaderstats; добавить явную инструкцию удалить отдельный leaderstats-скрипт из Фазы 5
- [x] 4.2 Добавить сохранение checkpoint: расширить data до {deaths, checkpoint}; загрузка восстанавливает lastCheckpoint/RespawnLocation (согласовать с CheckpointManager из Фазы 4)
- [x] 4.3 Добавить примечание о двойном сохранении BindToClose + PlayerRemoving при выключении сервера и способе избежать (флаг/проверка IsStudio)

## 5. phase-7-polish.md и phase-7-5-optimization.md

- [x] 5.1 Развести дубли ID звуков (чекпоинт/победа, смерть/урон) — разные rbxassetid с пометкой «заменить на свои»; шаг 1.2 перевести на game.SoundService согласно шагу 1.1
- [x] 5.2 Заменить «~20,000 Part'ов для стабильных 60 FPS» на качественное объяснение с пометкой «эмпирическая оценка»; смягчить совет «всегда WaitForChild» (таймауты, только где реально нужно)

## 6. phase-3-tween.md — комментарии и мёртвая зона

- [x] 6.1 Исправить комментарии в шаге 5.1 (Cancel = остановить анимацию; Play = запустить заново, не «переместить в конечную точку»)
- [x] 6.2 Круговая платформа: переименовать speed → segmentTime, добавить комментарий о периоде полного оборота; убрать бессмысленный task.wait(0)
- [x] 6.3 Добавить упражнение «мёртвая зона» из плана (Touched → Transparency + CanCollide=false)

## 7. roblox-learning-plan.md — синхронизация с фазами

- [x] 7.1 Фаза 5 мини-проект: убрать «меню паузы», таймер пометить как опциональное задание
- [x] 7.2 Фаза 5.5: уточнить формулировку чата («сервер получает и выводит», TextBox — задание со звёздочкой)
- [x] 7.3 Проверить остальные пункты чеклиста плана на покрытие фазами после правок 1–6

## 8. Верификация

- [x] 8.1 Пройтись по каждому гайду глазами новичка: все объекты/RemoteEvent'ы существуют до первого использования в коде
- [x] 8.2 Проверить перекрёстные ссылки между фазами (CheckpointManager ↔ DataStoreManager, кнопка ↔ RemoteEvent)
- [x] 8.3 Убедиться, что нумерация секций во всех изменённых файлах монотонна
